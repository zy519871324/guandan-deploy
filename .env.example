# docker-compose 环境变量
# 运行 scripts/deploy.sh 会自动生成 .env

JWT_SECRET=<自动生成，或运行: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))">
CLIENT_URL=http://localhost
NODE_ENV=production
JWT_EXPIRES_IN=7d
RATE_LIMIT_MAX=200
